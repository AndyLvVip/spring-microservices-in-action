server:
  port: 8888
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:./config_repo/eureka,file:./config_repo/zuul,file:./config_repo/licensing,file:./config_repo/organization